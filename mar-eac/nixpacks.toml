[phases.setup]
nixPkgs = ["openssl"]

[phases.install]
cmds = [
  "cd backend && npm ci --omit=dev",
  "cd frontend && npm ci"
]

[phases.build]
cmds = [
  "cd backend && npx prisma generate",
  "cd frontend && npm run build"
]

[variables]
VITE_API_URL = "/api"

[start]
cmd = "cd backend && node prisma/seed.js && node --max-http-header-size=32768 src/server.js"
